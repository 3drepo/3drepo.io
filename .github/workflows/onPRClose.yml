name: On PR closed
on:
  pull_request:
    types: [closed]
jobs:

  get-pr-number:
    name: Get Current PR Number
    runs-on: ubuntu-latest
    outputs: 
      pr-number: ${{ steps.condval.outputs.value }}
    steps:
      - uses: 3drepo/action-cond@v1
        id: condval
        with:
          cond: ${{ github.event_name == 'pull_request' }}
          if_true: ${{ github.event.number }}
          if_false: ${{ github.event.pull_request.number }}

  get-issue-number:
    name: Get Current Issue Number
    needs: [ get-pr-number ]
    runs-on: ubuntu-latest
    outputs: 
      issue-number: ${{ steps.commitMsgParser.outputs.issue-number }}
      content-id: ${{ steps.commitMsgParser.outputs.content-id }}
      merged: ${{ steps.commitMsgParser.outputs.merged }}
    steps:
      - name: Extract Issue Number from PR
        uses: 3drepo/extract-issue-number-from-PR@v1
        with:
          pr: ${{ needs.get-pr-number.outputs.pr-number }}
          base: staging
          github-token: ${{ secrets.GITHUB_TOKEN }}
        id: commitMsgParser

  tag-on-pr-merge:
    name: Tag merged issues as In staging if applicable
    runs-on: ubuntu-latest
    needs: get-issue-number
    steps:
      - name: Apply in staging label
        if: needs.get-issue-number.outputs.merged
        uses: carmenfan/any-issue-labeller@v1.1
        with:
          issue-number: ${{ needs.get-issue-number.outputs.issue-number }}
          label: 'in staging'
          github-token: ${{ secrets.GITHUB_TOKEN }}

  update-project-status:
    name: Update status of the issue in the project
    runs-on: ubuntu-latest
    needs: get-issue-number
    steps:
      - name: Update status in the Project
        id: update_status
        uses: 3drepo/update-project-action@1.1
        with:
          github_token: ${{ secrets.PROJ_MANAGEMENT_TOKEN }}
          organization: 3drepo
          project_number: 22
          content_id: ${{ needs.get-issue-number.outputs.content-id }}
          field: Status
          value: 'Done'

  destroy-deployed-branch:
    name: Call Azure Destroy Pipeline
    runs-on: ubuntu-latest
    needs: get-issue-number
    steps:
    - name: Azure Pipelines Action
      if: needs.get-issue-number.outputs.issue-number
      uses: Azure/pipelines@v1.2
      with:
        azure-devops-project-url: https://dev.azure.com/3drepo/3drepo.io
        azure-pipeline-name: 'destroy'
        azure-devops-token: ${{ secrets.AZURE_DEVOPS_TOKEN }}
        azure-pipeline-variables:  '{"branchName": "issue-${{ needs.get-issue-number.outputs.issue-number }}"}'